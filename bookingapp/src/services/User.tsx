/*
1. Create a User registration page where a new user can register a unique name and mail address
App waits for user confirmation using the code in @PostMapping("/user/findOrCreate")

2. User enter the code which is verified  @PostMapping("/verify") returning true.

3. User is redirected to App.tsx where he can book time using App.tsx 

4. An existing user can delete himself by enter the name and the code registered on the user using @DeleteMapping("/user")
*/
